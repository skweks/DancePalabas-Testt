# Frontend Source

This folder contains the React application source for StepSync AI.

## Location

- `src/frontend/` — React components, hooks, assets, and services.

## Notes

- The Vite entrypoint is now `index.html` -> `/src/frontend/main.jsx`.
- Keep all JSX, CSS, and client-side logic inside this directory.
